/**
 * Footer Component
 */
export function renderFooter() {
  return `
    <footer class="app-footer">
      <div class="layout-shell footer-container">
        <span>daily/ · deliberate task execution &amp; contemplative clarity</span>
        <span style="font-family: var(--font-mono); font-size: 0.6875rem; letter-spacing: 0.05em; color: var(--secondary);">
          local runtime active
        </span>
      </div>
    </footer>
  `;
}
